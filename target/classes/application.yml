spring:
  datasource:
    url: jdbc:postgresql://ep-green-haze-a40n2fsx-pooler.us-east-1.aws.neon.tech/neondb?sslmode=require
    username: neondb_owner
    password: npg_qvYj5Aai1RWX
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true

  jackson:
    default-property-inclusion: non_null

springdoc:
  api-docs:
    path: /api-docs
  swagger-ui:
    path: /swagger-ui.html
    tags-sorter: alpha

server:
  port: 85

logging:
  level:
    com.dsa.manager: DEBUG
    org.hibernate.SQL: DEBUG